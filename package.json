{
  "name": "megasoft-market",
  "version": "2.0.0",
  "private": true,
  "description": "Megasoft Market — parody B2B digital goods portal (Vue 3 + Vercel API)",
  "scripts": {
    "build": "npm run build --prefix shop && node scripts/stage-shop.mjs",
    "dev:shop": "npm run dev --prefix shop",
    "dev:api": "vercel dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "@types/node": "^22.19.0",
    "@vercel/node": "^5.5.6",
    "typescript": "^5.9.3"
  }
}
